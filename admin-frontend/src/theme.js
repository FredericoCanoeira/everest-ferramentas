import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#17C1E8" },
    secondary: { main: "#CB0C9F" },
    background: { default: "#f8f9fa" },
  },
});

export default theme;
